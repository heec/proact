import Proact from '@proact/core'

export default function (props, context) {
  const {} = props

  return (
    <header class="header">
      <div class="container">
        <a href="/">Proact SSG</a>
      </div>
    </header>
  )
}
